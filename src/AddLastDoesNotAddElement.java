/**
 * Broken implementation for testAddLastAddsElement.
 * @author Y
 * TODO: Y implements broken behavior so this test fails when SHOULD_FAIL is true.
 */
public class AddLastDoesNotAddElement<E> extends DoublyLinkedList<E> {
    @Override
    public void addLast(E e) {
        super.addLast(e); // TODO: replace with broken behavior
    }
}
