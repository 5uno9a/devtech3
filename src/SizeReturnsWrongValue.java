/**
 * Broken implementation for testSizeReturnsCorrectCount.
 * @author Y
 * TODO: Y implements broken behavior so this test fails when SHOULD_FAIL is true.
 */
public class SizeReturnsWrongValue<E> extends DoublyLinkedList<E> {
    @Override
    public int size() {
        return super.size(); // TODO: replace with broken behavior
    }
}
